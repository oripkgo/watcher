<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.watcher.mapper.MemberMapper">

	<insert id="insert" parameterType="memberVo" useGeneratedKeys="true" keyColumn="memId">

		INSERT INTO MEMBER(
			 MEM_ID
			,PASSWORD
			,EMAIL
			,PHONE_NUM
			,NAME
			,NICKNAME
			,MEM_TYPE
			,MEM_PROFILE_IMG
			,REG_ID
			,REG_DATE
			,UPT_ID
			,UPT_DATE
		)
		VALUES (
			 #{memId}
			,#{password}
			,#{email}
			,#{phoneNum}
			,#{name}
			,#{nickname}
			,#{memType}
			,#{memProfileImg}
			,#{memId}
			,NOW()
			,#{memId}
			,NOW()
		)
		ON DUPLICATE KEY UPDATE
			<if test="password != null and password != ''">
				PASSWORD 	= #{password} ,
			</if>

			<if test="email != null and email != ''">
				EMAIL 		= #{email} ,
			</if>

			<if test="phoneNum != null and phoneNum != ''">
				PHONE_NUM 	= #{phoneNum} ,
			</if>

			<if test="name != null and name != ''">
				NAME 		= #{name} ,
			</if>

			<if test="nickname != null and nickname != ''">
				NICKNAME 	= #{nickname} ,
			</if>

			<if test="memType != null and memType != ''">
				MEM_TYPE 	= #{memType} ,
			</if>

			UPT_ID 	= #{memId} ,
			UPT_DATE 	= NOW()
	</insert>

</mapper>