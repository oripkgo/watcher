<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.watcher.mapper.CategoryMapper">


	<select id="category_list" resultType="map" parameterType="categoryParam">
		/*category > category_list*/
		SELECT
			 ID
			,REG_ID
			,DATE_FORMAT(REG_DATE, '%Y.%m.%d %H:%i:%s') AS REG_DATE
			,UPT_ID
			,DATE_FORMAT(UPT_DATE, '%Y.%m.%d %H:%i:%s') AS UPT_DATE
			,CATEGORY_NM
			,CATEGORY_COMENTS
			,CATEGORY_IMG_PATH
		    ,SHOW_YN
			,SORT
		    ,'default' AS CATEGORY_TYPE
		FROM CATEGORY
		WHERE 1=1
		AND DELETE_YN = 'N'
		AND SHOW_YN = 'Y'
		ORDER BY SORT ASC

	</select>


	<select id="member_category_list" resultType="map" parameterType="categoryParam">
		/*category > member_category_list*/
		SELECT
			  ID
			 ,REG_ID
			 ,DATE_FORMAT(REG_DATE, '%Y.%m.%d %H:%i:%s') AS REG_DATE
			 ,UPT_ID
			 ,DATE_FORMAT(UPT_DATE, '%Y.%m.%d %H:%i:%s') AS UPT_DATE
			 ,DEFALUT_CATEG_ID
			 ,MEM_ID
			 ,CATEGORY_NM
			 ,CATEGORY_COMENTS
			 ,CATEGORY_IMG_PATH
			 ,SHOW_YN
			 ,'member' AS CATEGORY_TYPE
		FROM MEM_DEFINE_MENU_CATEG
		WHERE 1=1
		AND DELETE_YN = 'N'
		<if test="!(memId==null or memId=='')">
			AND MEM_ID = #{memId}
		</if>
		<if test="!(showYn==null or showYn=='')">
			AND SHOW_YN = #{showYn}
		</if>
	</select>





	<!--
	<insert id="insert" parameterType="memberParam" useGeneratedKeys="true" keyColumn="memId">

		INSERT INTO MEMBER(
			 MEM_ID
			,PASSWORD
			,EMAIL
			,PHONE_NUM
			,NAME
			,NICKNAME
			,MEM_TYPE
			,REG_ID
			,REG_DATE
			,UPT_ID
			,UPT_DATE
		)
		VALUES (
			 #{memId}
			,#{password}
			,#{email}
			,#{phoneNum}
			,#{name}
			,#{nickname}
			,#{memType}
			,#{memId}
			,NOW()
			,#{memId}
			,NOW()
		)
		ON DUPLICATE KEY UPDATE
			<if test="password != null and password != ''">
				PASSWORD 	= #{password} ,
			</if>

			<if test="email != null and email != ''">
				EMAIL 		= #{email} ,
			</if>

			<if test="phoneNum != null and phoneNum != ''">
				PHONE_NUM 	= #{phoneNum} ,
			</if>

			<if test="name != null and name != ''">
				NAME 		= #{name} ,
			</if>

			<if test="nickname != null and nickname != ''">
				NICKNAME 	= #{nickname} ,
			</if>

			<if test="memType != null and memType != ''">
				MEM_TYPE 	= #{memType} ,
			</if>

			UPT_ID 	= #{memId} ,
			UPT_DATE 	= NOW()
	</insert>
	-->

</mapper>